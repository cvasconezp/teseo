# Backend de Teseo — versiones probadas y compatibles entre sí.
#
# NOTA IMPORTANTE sobre astropy: hapsira (fork mantenido de poliastro)
# requiere astropy <6.0. Si actualizas astropy a una versión más nueva,
# hapsira dejará de funcionar (error: cannot import name 'matrix_product').
# Esto fue verificado experimentalmente — no es un supuesto.

fastapi==0.137.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2
httpx==0.28.1
groq==1.4.0

# Física orbital — NO actualizar astropy sin probar hapsira de nuevo
astropy==5.3.4
numpy==1.26.4
hapsira==0.18.0
